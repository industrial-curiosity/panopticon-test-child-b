# {Component name}

## Responsibility

{What this component owns, in one or two paragraphs. What is deliberately out of scope.}

## Interfaces

{Interfaces this component produces or consumes, by canonical index name, with a link to
[interfaces.md](../interfaces.md). Note which it owns.}

## Key modules

{The load-bearing source files/packages and what each does. Paths relative to the repo root.}

## Configuration

{Configuration this component reads (files, environment variables, flags), what each controls,
and required vs optional.}

## Failure modes

{How this component fails, what the blast radius is, and how failures surface (logs, metrics,
alerts).}
